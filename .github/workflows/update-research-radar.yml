name: Update research radar

on:
  schedule:
    # 06:15 every morning in Asia/Shanghai.
    - cron: "15 22 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Refresh PubMed items
        run: python scripts/update_literature.py --lookback-days 7

      - name: Commit changes
        run: |
          if [[ -n "$(git status --porcelain)" ]]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add data/research-items.json
            git commit -m "Update research radar data"
            git push
          else
            echo "No new research items found."
          fi
